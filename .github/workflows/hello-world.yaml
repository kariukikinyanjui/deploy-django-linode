name: Give a name anything

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  test_my_django_project:
    runs-on: ubuntu-latest
    env:
      DJANGO_SECRET_KEY: 'this-is-it'
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run hello World
        run: |
          echo "${{ env.DJANGO_SECRET_KEY }}"